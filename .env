# ============================================
# CONFIGURAÇÃO DO SISTEMA DE SINCRONIZAÇÃO
# ============================================
# 
# INSTRUÇÕES:
# 1. Defina o PERFIL_MAQUINA antes de iniciar o sistema
# 2. Configure o CAMINHO_GOOGLE_DRIVE para a pasta onde os backups serão salvos
# 
# ============================================

# Tipo de máquina (OBRIGATÓRIO)
# Valores permitidos: LOJA, GERENTE ou CADASTRO
# - LOJA: Acesso total + exporta backups para o Google Drive ao iniciar e fechar
# - GERENTE: Somente leitura + sincroniza (baixa) a versão mais recente do Google Drive
# - CADASTRO: Acesso total (exceto Contagem e Ocorrências) + exporta backups para o Google Drive
# Exemplo: PERFIL_MAQUINA=LOJA
PERFIL_MAQUINA=

# Nível de controle de estoque (OPCIONAL)
# Valores permitidos: CENTRAL, SETOR ou LOCAL
# - CENTRAL: Estoque único sem localização (padrão, como está hoje)
# - SETOR: Controle por setor (Almoxarifado, Produção, etc.)
# - LOCAL: Controle detalhado por setor + local (Prateleira A, Geladeira 1, etc.)
# Se não definido, usa CENTRAL ou o valor do banco de dados
# Exemplo: NIVEL_CONTROLE_ESTOQUE=SETOR
NIVEL_CONTROLE_ESTOQUE=

# Permitir estoque negativo (OPCIONAL)
# Valores permitidos: 0 (não permite) ou 1 (permite)
# - 0: Bloqueia movimentações que resultariam em estoque negativo (recomendado)
# - 1: Permite estoque negativo (útil para ajustes e correções)
# Se não definido, usa 0 (não permite) ou o valor do banco de dados
# Exemplo: PERMITIR_ESTOQUE_NEGATIVO=0
PERMITIR_ESTOQUE_NEGATIVO=0

# Exigir aprovação de gerente para movimentações (OPCIONAL)
# Valores permitidos: 0 (não requer/vai direto) ou 1 (requer aprovação)
# - 0: Movimentações são aplicadas imediatamente ao estoque (modo direto)
# - 1: Movimentações ficam pendentes até aprovação do gerente (modo auditado)
# Se não definido, usa 1 (requer aprovação)
# Exemplo: REQUER_APROVACAO_MOVIMENTACAO=1
REQUER_APROVACAO_MOVIMENTACAO=

# Caminho da pasta do Google Drive onde os backups serão salvos/sincronizados (OBRIGATÓRIO)
# IMPORTANTE: Use barras normais (/) em vez de barras invertidas (\)
# Exemplo: CAMINHO_GOOGLE_DRIVE=C:/Users/SeuUsuario/Google Drive/Backups Padaria
CAMINHO_GOOGLE_DRIVE=

    